# Public site URL (used for canonical URLs, sitemap, OG images)
NEXT_PUBLIC_SITE_URL=https://rashidalghafri.com

# Contact form (Phase 5) — server-side only, never exposed to the client
RESEND_API_KEY=
CONTACT_TO_EMAIL=
